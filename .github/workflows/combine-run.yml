name: Run Combine Scripts

on:
  push:
    branches: ['main']
  pull_request:
    branches: ['main']
  schedule:
    - cron: '0 20 * * *' # UTC 20:00 → 한국시간(KST) 05:00

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. 저장소 체크아웃
      - uses: actions/checkout@v4

      # 2. Git 설정
      - name: Set Git Config
        run: |
          git config --global user.email "webstoryboy@naver.com"
          git config --global user.name "websseu"

      # 3. Python 설정
      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.x'

      # 4. Python 패키지 설치
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt || pip install requests beautifulsoup4 lxml pandas selenium webdriver_manager

      # 5. 스크립트 실행
      - name: Run combine-youtube1.py
        run: python combine/combine-youtube1.py

      - name: Run combine-youtube2.py
        run: python combine/combine-youtube2.py

      # 6. 변경된 파일 확인 후 커밋 및 푸시
      - name: Commit and Push Changes
        run: |
          git add combine/combine-youtube.json korea/*.json
          git commit -m "🔄 Update JSON files (GitHub Actions)" || echo "No changes to commit"
          git push origin main
